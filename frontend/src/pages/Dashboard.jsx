function Dashboard() {
  const styles = {
    div: {
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      padding: "50px",
      fontFamily: "Helvetica",
    },
  };
  return (
    <div style={styles.div}>
      <h1>Dashboard</h1>
    </div>
  );
}

export default Dashboard;
